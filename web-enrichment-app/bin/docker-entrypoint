#!/bin/bash -e

# Ejecutar migraciones pendientes antes de arrancar (idempotente; rápido si no hay nada pendiente).
# Así cada deploy en Coolify/u otro deja la DB al día sin pasos manuales.
bundle exec rails db:prepare

exec "$@"
