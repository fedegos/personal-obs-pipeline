<% content_for :title, "Editar regla" %>

<div class="page-container">
  <header class="page-header">
    <h1 class="page-title">✏️ Editar Regla</h1>
    <p class="page-subtitle"><%= @category_rule.name %></p>
  </header>

  <%= render "form", category_rule: @category_rule %>

  <div class="category-rules-form-actions">
    <%= link_to "Ver regla", @category_rule, class: "btn-action-link" %>
    <%= link_to "Volver al motor de reglas", category_rules_path, class: "btn-action-link" %>
  </div>
</div>
