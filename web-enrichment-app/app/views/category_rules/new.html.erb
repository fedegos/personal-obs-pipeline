<% content_for :title, "Nueva regla" %>

<div class="page-container">
  <header class="page-header">
    <h1 class="page-title">➕ Nueva Regla</h1>
    <p class="page-subtitle">Agrega una regla de categorización (patrón, prioridad y sentimiento opcional).</p>
  </header>

  <%= render "form", category_rule: @category_rule %>

  <div class="category-rules-form-actions">
    <%= link_to "Volver al motor de reglas", category_rules_path, class: "btn-action-link" %>
  </div>
</div>
