<% content_for :title, "Regla: #{@category_rule.name}" %>

<div class="page-container">
  <% if notice.present? %>
    <p class="page-notice" id="notice"><%= notice %></p>
  <% end %>

  <header class="page-header">
    <h1 class="page-title">⚙️ <%= @category_rule.name %></h1>
    <p class="page-subtitle">Detalle de la regla de categorización.</p>
  </header>

  <%= render @category_rule %>

  <div class="category-rules-form-actions">
    <%= link_to "✏️ Editar", edit_category_rule_path(@category_rule), class: "btn-action-link" %>
    <%= link_to "Volver al motor de reglas", category_rules_path, class: "btn-action-link" %>
    <%= button_to "🗑️ Eliminar", @category_rule, method: :delete, class: "btn-action-delete", data: { turbo_confirm: "¿Eliminar esta regla?" } %>
  </div>
</div>
